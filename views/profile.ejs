<%- include("templates/header") %>
    <h1>User Profile</h1>

  <div class="user-profile">
    <h2><%= user.name %></h2>
    <h3><%= user.username %></h3>
    <h4><%= user.pronouns %></h4>
  </div>

  <div class="profile-actions">
    <button onclick="window.location.href='/profileDetails'">View Profile Details</button>
    <button onclick="window.location.href='/editProfile'">Edit Profile Details</button>
  </div>

    <!-- <form id="profile-form" action="/updateProfile" method="POST">
        <div>
            <label for="name">Edit Name:</label>
            <input type="text" id="name" name="name" value="<%= user.name %>" required>
        </div>


    </form>

    <div id="user-info">
        <h2>User Information</h2>
        <p><strong>Name:</strong> <%= user.name %></p>
        <p><strong>Username:</strong> <%= user.username %></p>
        <p><strong>Password:</strong> Hidden for security</p>
        <p><strong>Email:</strong> <%= user.email %></p>
        <p><strong>Age:</strong> <%= user.age %></p>
    </div>

    <button type="submit">Save Changes</button> -->



<%- include("templates/footer") %>
